import 'package:flutter/material.dart';

class VerDeportista extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Deportista'),
        ),
        body: Container(

        )
      );
  }
}